
  # Social Post App

  This is a code bundle for Social Post App. The original project is available at https://www.figma.com/design/qXWlDX7osoz3o55DAnskDq/Social-Post-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  